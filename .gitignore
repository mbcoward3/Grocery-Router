__pycache__/
*.pyc

# Cached by the prep job. Regenerated on demand, never the source of truth.
.cache/

# Built by build_static.py. The runtime it copies in lives in vendor/.
dist/

# Playwright MCP session snapshots.
.playwright-mcp/

# Task-local Talos credentials/state and uncommitted runtime secrets.
.local/
.env
*.secret.yaml
