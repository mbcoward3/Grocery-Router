apiVersion: v1
kind: Service
metadata:
  name: grocery-router
  namespace: grocery-router
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
      nodePort: 30080
